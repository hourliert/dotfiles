name: 2intercom-js
root: ~/src/intercom-js

on_project_start: if [ $(git rev-parse --abbrev-ref HEAD) == "master" ]; then git pl; fi

pre_window:
  - nvm use

startup_window: editor

windows:
  - server:
      layout: even-horizontal
      panes:
        - yarn && script/server
        - git fetch
  - editor: vim
  - test:
