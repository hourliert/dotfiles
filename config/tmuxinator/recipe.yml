name: 0recipe
root: ~/Projects/web-apps/my_recipe_box

on_project_start:
  - docker-compose up -d db cdn

on_project_stop:
  - docker-compose down

pre_window:
  - nvm use

startup_window: editor

windows:
  - server:
      layout: even-horizontal
      panes:
        - mix phx.server
        - ls
  - editor: vim
  - test:
